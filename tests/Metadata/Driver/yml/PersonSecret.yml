JMS\Serializer\Tests\Fixtures\PersonSecret:
    properties:
        name:
            type: string
        gender:
            type: string
            exclude_if: "show_data('gender')"
        age:
            type: string
            expose_if: "show_data('age')"
